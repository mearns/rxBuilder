version 'unspecified'

apply plugin: 'application'

mainClassName = 'org.ieee.bmearns.rxBuilder.example.Main'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.3.11'
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
